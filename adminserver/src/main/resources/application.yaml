server:
  port: 8888

spring:
  application:
    name: admin-server
  boot:
    admin:
      client:
        url: http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
